# 🚀 Telegram Announcement Funnel Bot

Автоматизированный Telegram бот для создания воронки анонсов с персонализированными HTML-шаблонами и интерактивными кнопками.

## 📋 Описание

Этот проект реализует полную автоматизацию трёхэтапной воронки анонсов через Telegram:

1. **🎯 Привлечение внимания** - эмоциональное вовлечение пользователя
2. **💡 Предложение решения** - рациональные аргументы и выгоды
3. **⏰ Создание срочности** - ограничение по времени для принятия решения

Бот генерирует персонализированные изображения на основе HTML-шаблонов и отправляет их пользователям с интерактивными кнопками для навигации по воронке.

## ✨ Особенности

- 🎨 **Персонализация** - автоматическая подстановка имени пользователя
- 🖼️ **Динамические изображения** - генерация картинок из HTML-шаблонов
- 📱 **Интерактивные кнопки** - плавная навигация по этапам воронки
- 💾 **Сохранение прогресса** - отслеживание текущего этапа пользователя
- 🎯 **Простая настройка** - легко изменяемые HTML-шаблоны
- 📊 **CSV база данных** - хранение информации о пользователях

## 🛠️ Технологии

- **Python 3.8+**
- **python-telegram-bot** - для работы с Telegram API
- **Pillow** - для создания изображений
- **python-dotenv** - для работы с переменными окружения

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/ai4bordon/telegram-announcement-funnel.git
cd telegram-announcement-funnel
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
```

### 3. Активация виртуального окружения

**Windows:**
```bash
.\venv\Scripts\Activate.ps1
```

**Linux/macOS:**
```bash
source venv/bin/activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 5. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_telegram_bot_token_here
```

## 🤖 Создание Telegram бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

## 🚀 Запуск

```bash
python funnel_bot.py
```

При успешном запуске вы увидите:
```
🚀 Бот запущен! Нажмите Ctrl+C для остановки
```

## 📁 Структура проекта

```
telegram-announcement-funnel/
├── funnel_bot.py              # Основной файл бота
├── requirements.txt           # Зависимости Python
├── .env                       # Переменные окружения (создайте сами)
├── .gitignore                 # Игнорируемые файлы Git
├── README.md                  # Документация проекта
├── users_data.csv             # База данных пользователей (создается автоматически)
├── templates/                 # HTML шаблоны для изображений
│   ├── stage1_interest.html   # Шаблон этапа 1
│   ├── stage2_solution.html   # Шаблон этапа 2
│   └── stage3_deadline.html   # Шаблон этапа 3
└── temp/                      # Временные файлы изображений (создается автоматически)
```

## 🎯 Использование

### Для пользователей

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Просматривайте этапы воронки, нажимая кнопку "Далее ➡️"

### Для разработчиков

#### Добавление нового этапа

1. Создайте новый HTML файл в папке `templates/`
2. Добавьте этап в код бота в функции `send_stage()`
3. Обновите максимальное количество этапов

#### Изменение дизайна

Редактируйте HTML файлы в папке `templates/`:
- Используйте переменную `{{name}}` для персонализации
- Размер изображения: 1080x1080 пикселей
- Поддерживается CSS стилизация

#### Настройка текстов

Измените тексты в функции `html_to_png()` в файле `funnel_bot.py`:

```python
if stage == 1:
    lines = [
        "🚀 Ты пропустил важное!",
        "",
        f"Привет, {user_name}!",
        # ... ваш текст
    ]
```

## 📊 База данных

Информация о пользователях сохраняется в файл `users_data.csv`:

```csv
name,telegram_id,current_stage
Иван,123456789,2
Мария,987654321,1
```

**Поля:**
- `name` - имя пользователя
- `telegram_id` - уникальный идентификатор Telegram
- `current_stage` - текущий этап воронки (1-3)

## 🔧 Настройка

### Изменение цветовой схемы

В функции `_draw_gradient_background()`:

```python
def _draw_gradient_background(self, draw, width, height):
    for y in range(height):
        r = int(102 + (118 - 102) * y / height)  # Красный
        g = int(126 + (75 - 126) * y / height)   # Зеленый
        b = int(234 + (162 - 234) * y / height)  # Синий
        draw.line([(0, y), (width, y)], fill=(r, g, b))
```

### Добавление новых кнопок

В функции `send_stage()`:

```python
keyboard = InlineKeyboardMarkup([
    [InlineKeyboardButton("Далее ➡️", callback_data="next_stage")],
    [InlineKeyboardButton("Подробнее", callback_data="more_info")]
])
```

## 🐛 Отладка

### Проверка логов

Бот выводит подробные логи в консоль:
- ✅ Успешные операции
- ✗ Ошибки и проблемы

### Частые проблемы

1. **"BOT_TOKEN не установлен"**
   - Проверьте файл `.env`
   - Убедитесь, что токен корректный

2. **"Template not found"**
   - Проверьте наличие HTML файлов в папке `templates/`

3. **"Chat not found"**
   - Пользователь должен сначала написать боту

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас есть вопросы или предложения:
- Создайте [Issue](../../issues)
- Напишите на email: your-email@example.com

## 🎉 Примеры использования

### Маркетинговая воронка
- Привлечение клиентов к новому продукту
- Продвижение услуг
- Сбор лидов

### Образовательные курсы
- Анонс новых курсов
- Приглашения на вебинары
- Продвижение обучающих программ

### E-commerce
- Анонс распродаж
- Продвижение новых товаров
- Сезонные предложения

---

**Сделано с ❤️ для автоматизации маркетинга в Telegram**
